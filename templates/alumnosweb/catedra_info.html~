<!--ESTE TEMPLATE ES PARA EL PRIMER PASO DEL FORMULARIO DE ALTA DE CÁTEDRAS. DEVUELVE LOS DATOS DE CATEDRAS -->
{% extends 'base.html' %}
{% block title %} Agregar una nueva cátedra {% endblock %}
	{% block header %} 
		{% include 'index.html' %}
		{% block alertmessage %}
			{% if messages %} 
    				
            			{% for message in messages %}  <!-- Para mostrar mensajes al usuario -->
              				<div class="alert alert-warning">
    						<strong>{{ message }}  </strong> 
    					</div>
           			 {% endfor %} 
			 {% endif %}
		{% endblock %}
	{% endblock %}
{% block content %}

<div class="container">
  	 <div class="row-fluid">  	 
		<div class="well clearfix" style="border-radius:5%;">
		<form class="" name="AgregarCatedra_step1" method="post">{% csrf_token %}
			<fieldset>
			    <legend>Ingrese datos de la cátedra </legend>
			        {% for field in form %}
			        	{% if field.errors %}
         				       <div class="col-sm-6 control-group error">
                					<label class="col-sm-4 control-label">{{ field.label }}</label> 
                					<div class="col-sm-8 controls ">{{ field }}
            					            <span class="help-inline">
          				                  {% for error in  field.errors %}{{ error }}{% endfor %}
                					    </span>
                                                        </div>
                                                </div>
                                        {% else %}
                                		<div class="col-lg-6 form-group">
              				  	      <label class="col-sm-4 control-label">{{ field.label }}</label> 
                    				      <div class="col-sm-8 controls">{{ field }}
                     					   {% if field.help_text %}
                				          <p class="help-inline"><small>{{ field.help_text }}</small></p>
                        			           {% endif %}
                    				      </div>
                				</div>
                		
            				{% endif %}
                		{% endfor %}
                	 </fieldset>
                	 	<small> Si la cátedra es sólo un día, llenar los aspectos teóricos.</small>
				<div class="pull-right form-actions">
					 <div class="btn-group ">
  						<button type="reset" class="btn btn-default"> <i class="fa fa-times"> </i> Borrar </button>
  						<button type="submit" class="btn btn-primary" value="save"> <i class="fa fa-check"></i> Guardar  </button>
  						
					</div>        				
    				</div>
			</form>
		</div>
	</div>
</div>
{% endblock %}
